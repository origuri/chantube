mixin video(info)
    div
        h4=info.title
        ul 
            li #{info.rating}/5
            li #{info.comments} comment.
            li Posted #{info.createdAt}
            li #{info.views} views